const [a, b, c, d] = [3, 5, 1, 15]

const soma = a + b + c + d // resultado de a + b é somado com c e resultado de a + b + c é somado com o d.
const subtracao = d - b
const multiplicacao = a * b
const divisao = d / a
const modulo = a % 2 // resto da divisão (vao dividir por 2 e mostrar o resto da divisão = 1)

console.log(soma, subtracao, multiplicacao, -divisao, modulo) // ao colocar "-" na frente da chave, vai se tornar um operador unario (menos = negativo)

